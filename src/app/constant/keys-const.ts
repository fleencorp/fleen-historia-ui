/**
 * Represents the key used for storing the authentication status in user data or storage.
 * @type {string}
 */
export const AUTHENTICATION_STATUS_KEY: string = 'AUTHENTICATION_STATUS';


/**
 * Represents the key used for storing the user's destination page in session storage.
 * It is typically used to store the page URL the user was trying to access before being redirected to the authentication page.
 * @type {string}
 */
export const USER_DESTINATION_PAGE_KEY: string = 'USER_DESTINATION_PAGE';
